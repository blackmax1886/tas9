#import User, NewUser from "user.graphql"
#import Task, NewTask from "task.graphql"

type Query {
  user(id: Int!): User
  tasks(userId: Int!): [Task!]!
  task(id: Int!): Task!
}

type Mutation {
  createUser(input: NewUser!): User!
  createTask(input: NewTask!): Task!
}
